import MapDiagram from './directions/MapDiagram.jsx';

// One page now. The three earlier directions (Component Playground, Long
// Document, Workbench) and the switcher that compared them are gone — Map /
// Diagram won, and keeping the losers around is how a repo grows two of
// everything.
export default function App() {
  return <MapDiagram />;
}
